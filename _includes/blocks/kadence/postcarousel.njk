{# kadence/postcarousel (PRO) — Post loop/carousel; replace with an Eleventy collection loop #}
{% set b = kadenceBlock.attrs %}
<section
  class="wp-block-kadence-postcarousel kb-post-carousel kadence-block kadence-postcarousel{% if b.uniqueID %} kb-post-carousel-id_{{ b.uniqueID }}{% endif %}{% if b.className %} {{ b.className }}{% endif %}"
>
  {# TODO: replace with Eleventy loop:
     {% for post in collections.posts | reverse | limit(b.postsToShow | default(3)) %}
       <article><a href="{{ post.url }}">{{ post.data.title }}</a></article>
     {% endfor %} #}
  {{ kadenceInnerHtml | safe }}
</section>
